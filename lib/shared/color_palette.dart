import 'package:flutter/material.dart';

class ColorPalette {
  static const primary = Color.fromRGBO(82, 135, 143, 1.0);

  static const secondary = Color.fromRGBO(242, 242, 242, 1.0);

  static const background = Color.fromRGBO(230, 230, 230, 1.0);

  static const mantis = Color.fromRGBO(144, 190, 81, 1.0);
}
